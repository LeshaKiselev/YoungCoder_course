#include <stdio.h>

int main(void) {
  int a, b, res;
  scanf("%d", &a);
  scanf("%d", &b);

  res = (a + b) * 2;

  int c;

  scanf("%d", &c);
  res = res;
  printf("%d", res);
  return 0;
}
